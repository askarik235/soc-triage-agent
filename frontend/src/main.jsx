import React, { useState } from "react"; import ReactDOM from "react-dom/client"; import App from "./App.jsx"; import Intro from "./Intro.jsx"; function Root() { const [entered, setEntered] = useState(false); return entered ? React.createElement(App) : React.createElement(Intro, { onEnter: () => setEntered(true) }); } ReactDOM.createRoot(document.getElementById("root")).render(React.createElement(React.StrictMode, null, React.createElement(Root)));